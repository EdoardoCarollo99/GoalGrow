namespace GoalGrow.Entity.Enums
{
    public enum RiskLevel
    {
        VeryLow = 1,   // Rischio molto basso (es: titoli di stato)
        Low = 2,       // Rischio basso (es: obbligazioni corporate AAA)
        Medium = 3,    // Rischio medio (es: fondi bilanciati)
        High = 4,      // Rischio alto (es: azioni growth)
        VeryHigh = 5   // Rischio molto alto (es: criptovalute, startup)
    }
}
