namespace GoalGrow.Entity.Enums
{
    public enum ChallengeStatus
    {
        InProgress = 1,     // In corso
        Completed = 2,      // Completata
        Failed = 3,         // Fallita (scaduta senza completamento)
        Abandoned = 4       // Abbandonata dall'utente
    }
}
