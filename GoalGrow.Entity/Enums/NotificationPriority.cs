namespace GoalGrow.Entity.Enums
{
    public enum NotificationPriority
    {
        Low = 1,        // Bassa priorità (può aspettare)
        Normal = 2,     // Priorità normale
        High = 3,       // Alta priorità (importante)
        Urgent = 4      // Urgente (notifica push immediata)
    }
}
