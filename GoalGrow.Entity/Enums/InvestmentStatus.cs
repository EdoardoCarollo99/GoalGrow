namespace GoalGrow.Entity.Enums
{
    public enum InvestmentStatus
    {
        Active = 1,     // Investimento attivo
        Sold = 2,       // Venduto/liquidato
        Suspended = 3,  // Sospeso
        Matured = 4     // Maturato (per obbligazioni/certificati)
    }
}
