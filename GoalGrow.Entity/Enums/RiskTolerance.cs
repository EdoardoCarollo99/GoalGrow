namespace GoalGrow.Entity.Enums
{
    public enum RiskTolerance
    {
        VeryConservative = 1,   // Molto conservativo (evita rischi)
        Conservative = 2,        // Conservativo (basso rischio)
        Moderate = 3,            // Moderato (rischio bilanciato)
        Aggressive = 4,          // Aggressivo (alto rischio)
        VeryAggressive = 5       // Molto aggressivo (rischio massimo)
    }
}
